services:
  crucix:
    build: .
    ports:
      - "${PORT:-3117}:${PORT:-3117}"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
